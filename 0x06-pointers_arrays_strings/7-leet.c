#include "main.h"
/**
 * leet - encodes string
 * @s: string
 *
 * Return: string
 */
char *leet(char *s)
{
	int count = 0;

	while (*(s + count) != '\0')
	{
		if (1)
		{
		}
		else if (1)
		{
		}
	}
	return (s);
}
