#include "main.h"
/**
 * _abs - Returns absolute value
 * Description: kk
 *
 * Return: value after absolute process
 */

int _abs(int number)
{
	if (number < 0)
	{
		return (number * -1);
	}
	return (number);
}
