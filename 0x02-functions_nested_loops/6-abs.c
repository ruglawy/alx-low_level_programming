#include "main.h"
/**
 * _abs - Returns absolute value
 * Description: kk
 * @number: number
 *
 * Return: value after absolute process
 */

int _abs(int number)
{
	if (number < 0)
	{
		return (number * -1);
	}
	return (number);
}
